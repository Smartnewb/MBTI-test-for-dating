/**
 * 애플리케이션 뷰포트 설정
 * 
 * Next.js 13.4 이상에서는 viewport 설정을 별도로 export 해야 합니다.
 */

export const viewport = {
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
  themeColor: '#6B3FA0',
};

export default viewport;
